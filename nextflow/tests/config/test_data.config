params {
    // Base directory for test data
    test_data_base = "${launchDir}/nextflow/tests/data"

    test_data {
        batchie {
            transcripts = "${params.test_data_base}/downsampled_transcripts.h5"
            boundaries = "${params.test_data_base}/downsampled_nucleus_boundaries.h5"
        }
    }
}
